/**
 * Phase B stub — cloud sync will be implemented when Supabase integration is added.
 * All calls are no-ops in Phase A.
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function syncCheckpoint(_repoPath: string, _tag: string): Promise<void> {
  // no-op in Phase A
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function syncRelease(_repoPath: string, _tag: string): Promise<void> {
  // no-op in Phase A
}
